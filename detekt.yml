build:
  maxIssues: 0

naming:
  VariableNaming:
    variablePattern: "(_)?[a-z][A-Za-z0-9]*"
    privateVariablePattern: '(_)?[a-z][A-Za-z0-9]*'
  ConstructorParameterNaming:
    excludes: [ '**/Response*.kt' ]
